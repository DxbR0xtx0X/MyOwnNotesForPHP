<?php
 //data_type_in_php
 /*
    boolean = True, False
    int = x
    float = any fraction we call it float x.y = 10.9
    string =
    array() =
    also we have built_in_function we gettype(True or 10 or 10.9 or string or ...)
    this function give us what data type we have
*/
$arrayName = array('one','two' );  or $arrayName =['one','two'] /* we can use the first method or the second one*/
// [] making this with ب in arabic lang
